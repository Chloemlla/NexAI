---
inclusion: always
---

# 工作流规范

- 每次完成代码更改后，必须立即编写 git commit 提交更改，不要等用户提醒。
- commit message 使用英文，格式：`type: 简要描述`（如 `fix:`, `feat:`, `refactor:`, `chore:`）。
- 如果修复了 GitHub issue/alert，在 commit message 中引用编号（如 `#460 #461`）。
