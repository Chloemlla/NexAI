<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NexAI.WinUI3.Views.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:notes="using:NexAI.Core.Notes"
    Background="Transparent">
    <Grid Padding="16" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock x:Name="NotesTitleText" Style="{StaticResource SubtitleTextBlockStyle}" Text="Notes" />
                        <TextBlock x:Name="NotesCountText" Opacity="0.72" Style="{StaticResource CaptionTextBlockStyle}" Text="0 notes" />
                    </StackPanel>
                    <Button x:Name="NewNoteButton" Grid.Column="1" Content="New" Style="{StaticResource AccentButtonStyle}" Click="NewNoteButton_Click" />
                </Grid>
                <TextBox x:Name="SearchBox" Grid.Row="1" Margin="0,12,0,12" PlaceholderText="Search notes" TextChanged="SearchBox_TextChanged" />
                <ListView x:Name="NotesList" Grid.Row="2" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="NotesList_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="notes:Note">
                            <StackPanel Padding="4,8" Spacing="2">
                                <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind Preview}" Opacity="0.72" Style="{StaticResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border
            Grid.Column="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TitleBox" Header="Title" PlaceholderText="Untitled Note" />
                    <Button x:Name="StarButton" Grid.Column="1" Content="Star" Click="StarButton_Click" />
                    <Button x:Name="DeleteButton" Grid.Column="2" Content="Delete" Click="DeleteButton_Click" />
                </Grid>
                <TextBox
                    x:Name="ContentBox"
                    Grid.Row="1"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    PlaceholderText="Write markdown notes with #tags and [[wiki-links]]..."
                    ScrollViewer.VerticalScrollBarVisibility="Auto" />
                <Grid Grid.Row="2" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" Opacity="0.72" VerticalAlignment="Center" />
                    <Button x:Name="SaveButton" Grid.Column="1" Content="Save" Style="{StaticResource AccentButtonStyle}" Click="SaveButton_Click" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
