<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NexAI.WinUI3.Views.Tools.ToolsHubPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NexAI.WinUI3.Views.Tools"
    Background="Transparent">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource TitleLargeTextBlockStyle}" Text="Tools" />
            <TextBlock Opacity="0.72" Text="Open a dedicated page for common tools, or use the generic host for the rest." TextWrapping="WrapWholeWords" />
        </StackPanel>

        <TextBox
            x:Name="SearchBox"
            Grid.Row="1"
            PlaceholderText="Search tools"
            TextChanged="SearchBox_TextChanged" />

        <GridView
            x:Name="ToolsGrid"
            Grid.Row="2"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="ToolsGrid_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:ToolListItem">
                    <Border
                        Width="220"
                        MinHeight="120"
                        Padding="14"
                        Margin="4"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <StackPanel Spacing="8">
                            <FontIcon Glyph="{x:Bind Glyph}" FontSize="20" />
                            <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="WrapWholeWords" />
                            <TextBlock Text="{x:Bind Description}" Opacity="0.72" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="WrapWholeWords" />
                            <TextBlock Text="{x:Bind Category}" Opacity="0.55" Style="{StaticResource CaptionTextBlockStyle}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
